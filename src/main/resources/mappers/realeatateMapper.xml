<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sudal.springtest.mybatis.repository.RealEatateRepository">
    <select id="selectRealEatate"
            resultType="com.sudal.springtest.mybatis.domain.RealEstate">
        SELECT
            `id`
             ,`realtor_id`
             ,`address`
             ,`area`
             ,`type`
             ,`price`
             ,`rent_price`
             ,`created_at`
             ,`updated_at`
        FROM
            `real_estate`
        WHERE
            `id` = #{id}
    </select>

    <select id="selectRealEatate1"
            resultType="com.sudal.springtest.mybatis.domain.RealEstate">
        SELECT
            `id`
            ,`realtor_id`
            ,`address`
            ,`area`
            ,`type`
            ,`price`
            ,`rent_price`
            ,`created_at`
            ,`updated_at`
        FROM
            `real_estate`
        WHERE
            `rent_price` <![CDATA[ <= ]]> #{rent}
    </select>

    <select id="selectRealEatate2"
            resultType="com.sudal.springtest.mybatis.domain.RealEstate">
        SELECT
            `id`
             ,`realtor_id`
             ,`address`
             ,`area`
             ,`type`
             ,`price`
             ,`rent_price`
             ,`created_at`
             ,`updated_at`
        FROM
            `real_estate`
        WHERE
            `area` <![CDATA[ >= ]]> #{area}
            AND
            `price` <![CDATA[ <= ]]> #{price}
    </select>
</mapper>